name: my_workflow

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build
        run: |
          echo "$vars.HAS_DB"
          echo "$secrets.DB_PASSWORD"
